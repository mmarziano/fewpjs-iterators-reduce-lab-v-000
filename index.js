const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

// Code your solution here

function reducer(batteries) {
   let totalBatteries = 0;
   return .reduce((total, element) => total += element, 0)
}

function batteries() {
  return batteryBatches;
}